const inquirer = require("inquirer");
const fs = require("fs");
const generateHTML = require('./src')

//create function that runs inquirer prompt questions.then() +> dont let code end and then run another inqprompt .then
